# Desire USB Creator release source
# For a zero-dependency Windows release, either:
#   1) put the prebuilt ISO under ..\payload\, OR
#   2) host the ISO and set download_url + sha256 below.
image_name=DesireVFIO-Fedora-44-KDE-x86_64.iso
download_url=
sha256=
